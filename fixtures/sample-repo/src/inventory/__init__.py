"""A small inventory/order pricing package — Lane 1 test fixture.

Not a real product. Exists to give A1/A2/A3 something realistic to read:
multiple modules, a real call graph, a genuinely long function, a bare
`except`, an unused import, and one deliberately wrong test assertion — so
the pipeline has real signals to detect and real evidence to cite, instead
of a synthetic one-file toy.
"""
